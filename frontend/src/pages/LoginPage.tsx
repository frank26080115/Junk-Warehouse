import React from "react";

const LoginPage: React.FC = () => (
  <div>
    <h1>Login</h1>
    {/* TODO: your form; on success, set cookie/session, redirect back */}
  </div>
);

export default LoginPage;
