import React from "react";

const ItemNewPage: React.FC = () => (
  <div>
    <h1>New Item</h1>
    <p>Blank template ready to insert.</p>
    {/* TODO: form bound to your Flask API */}
  </div>
);

export default ItemNewPage;
